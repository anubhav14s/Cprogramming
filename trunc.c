#include <stdio.h>
#include <math.h>

int main() {
    double a = 5.89;
    double b = -3.14;

    printf("trunc(%.2f) = %.2f\n", a, trunc(a));   
    printf("trunc(%.2f) = %.2f\n", b, trunc(b));   

    return 0;
}
